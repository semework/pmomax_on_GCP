apiVersion: v1
kind: Service
metadata:
  name: pmomax-pid-architect
  labels:
    app: pmomax-pid-architect
spec:
  type: LoadBalancer
  selector:
    app: pmomax-pid-architect
  ports:
    - name: http
      port: 80
      targetPort: 8080

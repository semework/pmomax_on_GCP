apiVersion: apps/v1
kind: Deployment
metadata:
  name: pmomax-pid-architect
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: app
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
